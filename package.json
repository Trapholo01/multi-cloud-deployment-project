{
  "name": "portfolio-app-azure",
  "version": "1.0.0",
  "description": "Multi-cloud portfolio application with Gemini API - Azure Deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "install-deps": "npm install",
    "build": "echo \"No build process required\"",
    "health-check": "curl -f http://localhost:8080/health || exit 1"
  },
  "keywords": [
    "azure",
    "nodejs",
    "express",
    "gemini-api",
    "multi-cloud"
  ],
  "author": "Your Team Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "path": "^0.12.7",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}